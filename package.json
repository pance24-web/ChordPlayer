{
  "name": "chordplayer",
  "version": "1.0.0",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "build": "echo 'No build step required'",
    "lint": "node --check server.js && node --check script.js && node --check api/songs.js && node --check api/song-detail.js && node --check services/songService.js && node --check config/db.js",
    "test": "node --test"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^5.2.1",
    "mysql2": "^3.11.0"
  }
}